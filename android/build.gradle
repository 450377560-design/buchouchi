buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // 保留空或仅用于极旧依赖；Flutter 新模板不需要在这里声明 AGP/Kotlin
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
