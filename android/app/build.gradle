buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // 空即可；AGP/Kotlin 已在 settings.gradle 的 plugins 块声明
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
